#include "monty.h"
/**
 * nop - function
 * @stack: pointer
 * @line_number: integer
 * Return: void
 */
void nop(stack_t **stack, unsigned int line_number)
{
	if (stack == NULL)
	{}
	if (line_number > 0)
	{}
}
